// =============================================================================
// abs.mlir —— 最小模型：一个 op，一个 dispatch
// -----------------------------------------------------------------------------
// 用途：【逐相位精读】。它小到每一相 dump 出来都能整篇看完，
// 所以适合回答「这一相位新增了哪一类信息」。
//
// 想看融合与 dispatch 划分，请用 models/tiny_mlp.mlir —— 单个 op 演示不了那些。
//
// 对应 docs/learning-guides/iree-learning-guide.md 第 1–2 章。
// =============================================================================

func.func @abs(%input : tensor<f32>) -> tensor<f32> {
  %result = math.absf %input : tensor<f32>
  return %result : tensor<f32>
}
